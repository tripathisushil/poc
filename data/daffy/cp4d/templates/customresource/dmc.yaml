apiVersion: dmc.databases.ibm.com/v1
kind: Dmcaddon
metadata:
  name: dmc-cr                                  # This is the recommended name, but you can change it
  namespace: @CP4D_INSTANCE_NAMESPACE@          # Replace with the project where you will install Db2 Data Management Console
spec:
  license:
    accept: true
    license: Enterprise          # Specify the license you purchased
  version: @CP4D_VERSION@
