apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ocs-operator
  namespace: openshift-storage
spec:
  channel: stable-@OCS_OPERATOR_VERSION@
  name: ocs-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
